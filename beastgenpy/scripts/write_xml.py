import glm_funcs as glm
import taxon_set_funcs as taxon_set

def write_xml():

    xml_file.write('<beast>\n\t<taxon id="origin">\n\t\t<date value="2016.0" direction="forwards" units="years"/>\n\t</taxon>")










loc_list = list(centroid_dict.keys())

#will replace keys in centroid dict with the right number code
count = 0
for loc in loc_list:
    random_matrix_prolif(loc, count)
    count += 1