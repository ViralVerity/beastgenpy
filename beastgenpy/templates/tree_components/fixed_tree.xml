<!-- Insert fixed tree blocks -->  
%for name, info in config["sequence_info"].items():
	<empiricalTreeDistributionModel id="${name}.treeModel" fileName="${info['tree_file']}">
		<taxa idref="${name}.taxa"/>
	</empiricalTreeDistributionModel> 
%endfor