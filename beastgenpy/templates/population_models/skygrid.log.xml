%for name in config["sequence_info"].keys():
            <parameter idref="${name}.skygrid.precision"/>
			<parameter idref="${name}.skygrid.logPopSize"/>
			<gmrfSkyGridLikelihood idref="${name}.skygrid"/>
%endfor